# Client environment. Copy to .env.local — never commit real values.
# The client currently ships a mock service layer; when the API lands,
# point this at it and swap client/src/services/api.js (the only seam).

# VITE_API_URL=http://localhost:5000/api
# VITE_SOCKET_URL=http://localhost:5000

# Demo mode: simulate market data locally instead of using a live feed.
VITE_DEMO_FEED=true
